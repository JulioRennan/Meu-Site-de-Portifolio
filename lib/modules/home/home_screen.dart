import 'package:flutter/material.dart';
import 'body_home_web.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const BodyHomeWeb();
  }
}
